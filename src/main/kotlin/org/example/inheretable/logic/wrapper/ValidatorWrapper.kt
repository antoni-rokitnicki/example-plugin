package org.example.inheretable.logic.wrapper

import org.example.inheretable.IEnumValidator

class ValidatorWrapper {
    fun wrap(ev : IEnumValidator) : ValidatorWrapper {
        //TODO: wrap IEnumValidator in class needed by plugin
        return this
    }
}